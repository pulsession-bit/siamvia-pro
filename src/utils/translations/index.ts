// Auto-generated index file for translations
// Each language is now in its own file for better performance and maintainability

import { fr } from './fr';
import { en } from './en';
import { de } from './de';
import { es } from './es';
import { it } from './it';
import { th } from './th';
import { ru } from './ru';
import { zh } from './zh';
import { ja } from './ja';
import { ko } from './ko';
import { ar } from './ar';

export const translations = {
    fr,
    en,
    de,
    es,
    it,
    th,
    ru,
    zh,
    ja,
    ko,
    ar
};

export type Language = keyof typeof translations;
export type Translation = typeof translations.fr;
